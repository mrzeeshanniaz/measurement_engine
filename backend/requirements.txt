fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Image processing
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.0
scipy>=1.13.0

# Pose detection
mediapipe>=0.10.14

# ML / mesh
torch>=2.3.0
torchvision>=0.18.0
smplx>=0.1.28
trimesh>=4.3.0

# Firebase / Firestore (optional — persistence disabled when credentials are absent)
firebase-admin>=6.5.0

# Production hardening
slowapi>=0.1.9              # per-IP rate limiting
prometheus_client>=0.20.0   # /metrics endpoint
gunicorn>=22.0.0            # production WSGI/ASGI runner

# Dev
pytest>=8.2.0
httpx>=0.27.0
